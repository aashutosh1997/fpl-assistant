"""Option values: what flexibility is worth, measured from the projection panel.

A banked transfer, money in the bank, a bench place, a held chip and the weeks beyond the
planning horizon are all options — the right to react to information that has not arrived yet.
Their worth depends on how much the projections move between one deadline and the next, which
:mod:`fplass.options.revisions` measures, and on the decision that movement would change, which
:mod:`fplass.options.value` prices by re-solving under sampled futures.
"""
